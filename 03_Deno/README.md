# DENO REST API

### API END POINTS
-  **`GET`**  - /todos
-  **`POST`** - /todos (form data)
-  **`PUT`**  - /todos/:id (form data)
-  **`DELETE`** - /todos/:id 

**RUN**
```
deno run --allow-net -allow-read --allow-write index.js

```